using Gtk 4.0;

template $AppButton: Gtk.Button {
	css-classes: [
		"bar-item",
	];

	hexpand: true;

	Overlay {
		child: Box {
			orientation: horizontal;
			spacing: 5;

			Image app_icon {
				icon-size: large;
			}
		};

		[overlay]
		Box indicator {
			css-classes: [
				"indicator",
			];
		}
	}
}
